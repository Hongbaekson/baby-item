# 이은이 육아템 개발 계획

작성일: 2026-07-01 (KST)

## 1. 목표

공개 Notion DB에 정리된 육아템 데이터를 분석해, 별도 사이트 **이은이 육아템**으로 옮긴다. 1차 목표는 로그인 없는 공개 조회 사이트이며, DNS 구매 없이 OCI 인스턴스의 public IP로 누구나 접근할 수 있게 배포한다.

## 2. 원본 Notion 분석

원본 URL:

https://bedecked-shingle-38a.notion.site/248b4abb08ed803e8825db3d5713f396?v=248b4abb08ed8094905c000cd1938a4c

확인된 구조:

- 페이지 타입: Notion collection view page
- 컬렉션명: `깡깡이 아빠가 찐으로 가장 많이 사용한 육아템`
- 기본 뷰: `카테고리별 보드`
- 총 제품 행: 47개
- 주요 필드:
  - `제품명`
  - `수유아이템` 필드명으로 보이는 카테고리 select
  - `파트너스 링크`
  - `가격`
  - `상태`
  - `메모`
  - `파일과 미디어`

카테고리별 행 수:

| 카테고리 | 행 수 |
| --- | ---: |
| 👶300일간 매일 사용한 육아템 정리 | 9 |
| 💤수면 아이템 | 6 |
| 😎외출 아이템 | 8 |
| 🍼젖병 열탕 소독 | 5 |
| 🍼수유아이템 | 4 |
| 💩신생아 배앓이 꿀템 | 3 |
| 🎉놀이아이템 | 4 |
| 💩배변아이템 | 5 |
| 👶거실매트 | 1 |
| 🧑‍🍼손목&허리보호대(양육자를 위한 아이템) | 2 |

참고: Notion API의 `sizeHint`는 49로 내려오지만, 실제 카테고리 그룹에 속한 제품 페이지 합계는 47개다. 현재 추출 기준은 사용자 화면에 노출되는 카테고리 그룹의 제품 페이지다.

주요 제품 예시:

- 수유: 베이비 브레짜 분유제조기, 보르르 분유포트, 꿈비 분유쉐이커, 유팡 젖병 살균기
- 수면: 리안드림콧 아기침대, 말랑하니 백색소음기, 휴비딕 온습도계, 뮤라 방수패드
- 배변: 소베맘 기저귀갈이대, 포프베베 아기비데, 매직캔 휴지통, 롤베이비 방수패드
- 외출: 줄즈 데이 5 유모차, 아이엔젤 아기띠, 기저귀 가방, 휴대용 기저귀 매트
- 놀이: 타이니모, 바람개비 아기 체육관, 역류방지쿠션

데이터 정리 이슈:

- 제목이 비어 있는 행이 2개 있다. MVP에서는 숨기고, 데이터 정리 단계에서 삭제/보완 여부를 결정한다.
- 같은 제품이 여러 카테고리에 중복된다. 예: 꿈비 분유쉐이커, 보르르 분유포트, 헤이홈 홈캠, 소베맘 기저귀갈이대.
- 제품명이 약간씩 다르다. 예: `말랑하니 백색소음` / `말랑하니 백색소음기`.
- 가격이 있는 제품과 없는 제품이 섞여 있다.
- `bit.ly/4jmx2HM`처럼 프로토콜이 빠진 링크가 있다. 사이트 데이터에서는 `https://`를 보정한다.
- `리안드림콧 아기침대` 메모에 무선 이어폰 관련 문장이 들어가 있어 정정이 필요하다.
- 파트너스 링크가 포함되어 있으므로 사이트 상단 또는 푸터에 제휴 링크 고지를 반드시 노출한다.
- Notion 첨부 이미지는 임시 서명 URL일 수 있어, 배포 전에는 로컬/OCI 정적 파일로 캐싱하는 방향이 안전하다.

## 3. 권장 기술 방향

1차 MVP는 **정적 프론트엔드 + Nginx 배포**로 간다.

- 앱: Vite + React + TypeScript
- 데이터: 빌드 타임 JSON 파일
- 스타일: CSS Modules 또는 단일 CSS. 초기에는 Tailwind 도입 없이 가볍게 시작
- 배포: Docker 이미지 안에 빌드 결과물을 넣고 Nginx로 서빙
- 운영: OCI VM에서 Docker Compose로 실행
- 접근: `http://<OCI_PUBLIC_IP>` 형태

이 방향이 맞는 이유:

- 현재 요구사항은 공개 조회가 핵심이라 DB/API 서버가 당장 필요 없다.
- DNS 없이 public IP 접근만 할 경우 정적 사이트가 가장 단순하고 장애 지점이 적다.
- 추후 관리자 페이지, 가격 갱신, 클릭 통계가 필요해지면 API 서버와 DB를 MVP 이후에 붙이면 된다.

## 4. 사이트 기능 범위

MVP에 포함:

- 첫 화면에서 바로 제품 목록 노출
- 브랜드명: `이은이 육아템`
- 카테고리 탭/칩 필터
- 제품명 검색
- 가격 표시
- 가격 미입력 제품은 `가격 확인 필요`로 표시
- 제품 카드: 이미지, 제품명, 카테고리, 가격, 한 줄 메모, 구매 링크 버튼
- 제품 상세 모달 또는 상세 페이지
- 제휴 링크 고지
- 모바일 우선 반응형 UI
- 아이들이 보는 것처럼 아기자기한 파스텔 톤 디자인
- 둥근 카드, 부드러운 그림자, 귀여운 카테고리 아이콘/스티커 느낌의 시각 요소
- 빈 제목/링크 오류/가격 누락을 데이터 검수 상태로 표시

MVP에서 제외:

- 로그인
- 관리자 페이지
- 실시간 가격 크롤링
- 결제
- 사용자 리뷰/댓글
- 자동 Notion 동기화
- HTTPS 강제 적용

## 5. Phase / Step 계획

### Phase 0. 기준 확정

목표: 개발 범위와 배포 방식을 확정한다.

- Step 0.1: 사이트 이름, 공개 문구, 제휴 고지 문구 확정
- Step 0.2: 원본 링크를 그대로 쓸지, 본인 파트너스 링크로 교체할지 결정
- Step 0.3: OCI 접근 방식 확정: `http://public-ip` 1차 공개

완료 조건:

- `docs/development-plan.md` 기준으로 MVP 범위가 확정된다.

### Phase 1. 데이터 정규화

목표: Notion 데이터를 앱에서 쓰기 좋은 JSON으로 만든다.

- Step 1.1: Notion 제품 행을 `data/items.raw.json`으로 추출
- Step 1.2: 빈 제목 행 제거 또는 `draft` 처리
- Step 1.3: 중복 제품을 canonical item으로 묶고, category는 배열로 정규화
- Step 1.4: 링크 프로토콜 보정
- Step 1.5: 가격 누락, 메모 오류, 이미지 누락 상태를 `dataQuality` 필드로 표시
- Step 1.6: 정리된 결과를 `src/data/items.json`으로 생성

완료 조건:

- 앱이 외부 Notion 호출 없이 로컬 JSON만으로 렌더링할 수 있다.
- 빈 제목 제품은 사용자 화면에 노출되지 않는다.

### Phase 2. 비주얼 디자인 방향 확정

목표: `이은이 육아템`의 첫인상을 아기자기하고 따뜻한 육아 사이트로 고정한다.

- Step 2.1: 파스텔 컬러 팔레트 확정
- Step 2.2: 카테고리별 아이콘/스티커 톤 정의
- Step 2.3: 제품 카드, 검색창, 필터 칩의 둥근 정도와 그림자 스타일 정의
- Step 2.4: 모바일 첫 화면에서 브랜드명, 검색, 카테고리, 추천 제품이 자연스럽게 보이도록 레이아웃 와이어프레임 작성
- Step 2.5: 너무 유치하거나 산만하지 않도록 텍스트 가독성과 버튼 대비 확인

완료 조건:

- 구현 전에 사용할 컬러, 카드 스타일, 아이콘 톤, 모바일 레이아웃 기준이 정리된다.
- 디자인은 밝고 귀엽지만 제품 탐색과 구매 링크 클릭이 방해받지 않는다.

### Phase 3. 로컬 MVP 구현

목표: 로컬에서 바로 볼 수 있는 `이은이 육아템` 사이트를 만든다.

- Step 3.1: Vite + React + TypeScript 프로젝트 생성
- Step 3.2: 제품 데이터 타입 정의
- Step 3.3: 카테고리 필터, 검색, 가격 표시 구현
- Step 3.4: 제품 카드 UI 구현
- Step 3.5: 제품 상세 모달/페이지 구현
- Step 3.6: 제휴 링크 고지와 푸터 구현
- Step 3.7: 모바일/데스크톱 반응형 확인

완료 조건:

- `npm run dev`로 로컬 사이트 접속 가능
- 모바일 폭에서 텍스트/버튼 겹침 없음
- 제품 링크 버튼이 정상 동작

### Phase 4. 이미지 이관

목표: Notion 이미지 의존도를 제거한다.

- Step 4.1: Notion 첨부 이미지 URL 확인
- Step 4.2: 사용 가능한 이미지를 `public/images/items`에 저장
- Step 4.3: 이미지 없는 제품은 카테고리별 기본 이미지 또는 심플 플레이스홀더 적용
- Step 4.4: 이미지 경로를 JSON에 반영

완료 조건:

- 배포 후에도 이미지가 Notion 세션/서명 URL 만료에 영향받지 않는다.

### Phase 5. 품질 검증

목표: 공개 배포 전 깨지는 부분을 막는다.

- Step 5.1: `npm run build`
- Step 5.2: 주요 viewport 스크린샷 확인
- Step 5.3: 링크 형식 검증
- Step 5.4: 데이터 품질 리포트 출력
- Step 5.5: public IP 공개 전 민감정보가 없는지 확인

완료 조건:

- 빌드 성공
- 깨진 내부 라우트 없음
- 화면상 겹침 없음
- `.env`나 개인 토큰이 repo/이미지에 포함되지 않음

### Phase 6. OCI 배포

목표: OCI VM public IP로 누구나 사이트에 접속하게 한다.

- Step 6.1: Dockerfile 작성: React build 결과를 Nginx로 서빙
- Step 6.2: `docker-compose.yml` 작성
- Step 6.3: OCI VM에 코드 또는 빌드 산출물 배포
- Step 6.4: OCI Security List 또는 NSG에서 80 포트 ingress 허용
- Step 6.5: VM 방화벽에서 80 포트 허용
- Step 6.6: `http://<OCI_PUBLIC_IP>` 접속 확인
- Step 6.7: 컨테이너 재시작 정책 설정

완료 조건:

- 외부 네트워크에서 `http://<OCI_PUBLIC_IP>` 접속 가능
- 서버 재부팅 후 컨테이너가 자동 복구된다.

### Phase 7. 운영 개선

목표: 운영 중 업데이트가 쉬운 상태로 만든다.

- Step 7.1: 데이터 업데이트 절차 문서화
- Step 7.2: 배포 명령 문서화
- Step 7.3: 링크 오류/가격 누락 체크 스크립트 추가
- Step 7.4: 필요하면 관리자 페이지/API/DB 도입 여부 재검토

완료 조건:

- 새 제품 추가나 가격 수정 시 어떤 파일을 바꾸고 어떤 명령을 실행할지 명확하다.

## 6. OCI 주의사항

- DNS를 구매하지 않으면 기본 접속 주소는 `http://<public-ip>`가 된다.
- 일반 사용자용 신뢰 HTTPS는 도메인이 있을 때 훨씬 단순하다. IP만으로 HTTPS를 하려면 인증서 발급/브라우저 신뢰 문제가 생길 수 있으므로 MVP에서는 HTTP 공개를 기준으로 한다.
- public IP 공개 서버이므로 최소한 80 포트만 열고, SSH는 본인 IP 제한을 권장한다.
- 앱에는 토큰, Notion 쿠키, 파트너스 비밀키를 넣지 않는다.

## 7. 현재 완료 상태

2026-07-01 기준, OCI 서버 접속/배포 직전까지의 로컬 준비를 완료했다.

완료:

- Phase 1 데이터 정규화 완료
- Phase 2 비주얼 디자인 기준 문서화 완료: `docs/visual-design.md`
- Phase 3 로컬 MVP 구현 완료
- Phase 4 Notion 이미지 의존 제거 완료: 카테고리별 SVG placeholder 적용
- Phase 5 품질 검증 완료
- OCI 배포 준비 파일 작성 완료: `Dockerfile`, `docker-compose.yml`, `nginx.conf`

현재 검증 결과:

- 앱 노출 제품: 37개
- 링크 형식 오류: 0개
- 로컬 이미지 파일 누락: 0개
- 데이터 검증 실패: 0개
- 프로덕션 빌드: 성공
- 검수 필요 제품: 1개 (`리안드림콧 아기침대` 메모 정정 필요)

로컬 실행:

```bash
npm run dev
```

프로덕션 빌드 확인:

```bash
npm run data:check
npm run build
npm run serve:dist
```

Docker 설정 확인:

```bash
docker compose config
```

## 8. 다음 실행 제안

다음 작업은 **Phase 6 OCI 배포**이다. 실제 서버 작업 전에 아래를 확인한다.

- `ssh oci` 접속 후 이 프로젝트를 `h-log`와 다른 경로에 둔다. 예: `/opt/euni-baby-items`
- `h-log`가 80 포트를 쓰고 있는지 확인한다.
- 80 포트가 비어 있으면 `APP_PORT=80`으로 배포한다.
- 80 포트가 이미 사용 중이면 `APP_PORT=8080` 같은 별도 포트를 열거나, 기존 Nginx reverse proxy에 라우팅을 추가한다.
- OCI Security List 또는 NSG에서 실제 사용할 포트를 열기 전까지는 외부 공개하지 않는다.
